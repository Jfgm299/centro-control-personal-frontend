import { Home } from 'lucide-react'
import HomePage from './HomePage'

export default {
  id: 'home',
  labelKey: 'nav.home',
  icon: Home,
  path: '/',
  color: '#f59e0b',
  component: HomePage,
  permanent: true,
  iconType: 'lucide',
}